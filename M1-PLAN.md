# LocalMCP 기반 모듈형 코딩 실행환경 — M1

기준 원본: `daodao97/localmcp@fff33cfb5249be914ec7bab54455e793c1617321`
대상 포크: `dearcloud09/localmcp`

## 목표

ChatGPT가 판단하고 로컬 실행환경이 파일 수정과 검증을 담당한다. LocalMCP의 연결 기반을 재사용하고 개발 기능은 교체 가능한 모듈로 분리한다. 모듈화가 별도의 프로세스나 MCP 서버를 의무화하지는 않는다.

```text
ChatGPT → MCP 입구 → 공통 실행 코어 → 기능 모듈
                                     ├ 내부 함수
                                     ├ 기존 CLI
                                     └ 선택적 하위 MCP
```

## 역할과 원칙

공통 코어는 도구 등록, 입력 검증, 기능 활성화 확인, 호출 결과와 실행 기록을 관리한다. 파일 편집은 내부 코드, Git·테스트·빌드는 기존 CLI를 우선 활용한다. 하위 MCP는 전문 기능이 필요할 때만 추가한다. 자주 쓰는 기능은 모델이 직접 호출할 수 있게 하고 불필요한 발견·왕복을 줄인다.

## M1 구현 범위

- 공통 도구 레지스트리와 workspace·files·processes·Skills·external MCP·Git 모듈 분리.
- 실행 파일과 인자를 분리하는 내부 CLI 실행기 및 읽기 전용 Git 상태 도구.
- 기존 26개 도구의 입력 스키마 유지, `git_status` 하나 추가.
- 파일 덮어쓰기의 일반 권한 비트 보존, 분할 UTF-8 출력과 비동기 프로세스 오류 처리.
- 독립 테스트, SDK 통합 테스트, 적용 패치와 검증 기록.

## 완료 기준

공통 코어를 통해 모듈이 호출되고 비활성 기능·잘못된 입력이 실행 전에 거부되어야 한다. 실제 임시 Git 저장소와 파일·프로세스 회귀 테스트를 통과해야 한다. 전체 checkout에서 원본 의존성으로 타입 검사·빌드·전체 테스트를 통과하고, 별도로 ChatGPT의 읽기 → 수정 → 테스트 → diff 확인을 검증한다.

독립 테스트 통과와 전체 통합·ChatGPT 연결 성공은 서로 다른 판정이다. 성공하지 않은 검증을 완료로 표시하지 않는다.

## 후속 범위

M2: 프로젝트 한정 초기 설정, 세부 권한과 OS 격리, 수정 충돌, 작업 복구, 기록 영속화.
M3: 실제 과제에서 호출 수·재시도·완료시간을 측정한 뒤 배치 처리와 전문 기능 추가.
자체 IDE·언어 서버·다중 에이전트·브라우저 자동화·자동 배포는 M1 범위 밖이다.

## 실행 경계

`main`과 업스트림은 직접 수정하지 않는다. 공개 터널, 실제 홈 디렉터리 연결, 토큰 공유, 패키지 배포는 자동 실행하지 않는다. M1의 기능 플래그와 로그를 완성된 보안 경계나 OS 샌드박스로 간주하지 않는다. 원본 MIT 고지를 유지한다.

**이 파일은 계획이다. 코드의 원격 반영 및 검증 상태는 `docs/m1-remote-status.md`를 확인한다.**
